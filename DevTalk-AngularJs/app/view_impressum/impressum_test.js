'use strict';

describe('DevTalk.impressum module', function() {

  beforeEach(module('DevTalk.impressum'));

  describe('impressum controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var impressumCtrl = $controller('ImpressumCtrl');
      expect(impressumCtrl).toBeDefined();
    }));

  });
});