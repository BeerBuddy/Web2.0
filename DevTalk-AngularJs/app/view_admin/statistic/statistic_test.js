'use strict';

describe('DevTalk.statistic module', function() {

  beforeEach(module('DevTalk.statistic'));

  describe('statistic controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var statisticCtrl = $controller('StatisticCtrl');
      expect(statisticCtrl).toBeDefined();
    }));

  });
});