'use strict';

describe('DevTalk.editTalk module', function() {

  beforeEach(module('DevTalk.editTalk'));

  describe('editTalk controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var editTalkCtrl = $controller('EditTalkCtrl');
      expect(editTalkCtrl).toBeDefined();
    }));

  });
});